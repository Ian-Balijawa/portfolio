/** @type {import('next').NextConfig} */
const nextConfig = {
   // image optimization
   images: {
      domains: [
         'placehold.it',
         'media.licdn.com',
      ],
   },
}

module.exports = nextConfig
